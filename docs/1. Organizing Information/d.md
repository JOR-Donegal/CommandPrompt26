# Create a file from the console

Sometime it can be useful to be able to crete a test file from the command prompt. This is very old school!

<figure>
<img src = "https://jor-donegal.github.io/CommandPrompt26/images/fig3.jpg">
<figcaption>Fig 3. Version.</figcaption>
</figure>

The copy command is always in the form __copy__ _source destination_

In this case, the source is the console and the destination is a filename in the working directory.